{
    "name": "@openuji/speculator-monorepo",
    "private": true,
    "scripts": {
        "test": "pnpm -r test",
        "build": "pnpm -r build"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    }
}
# spec-page example

Minimal single-document publishing sample for `@openuji/spec-page`.

## Render

```bash
pnpm --filter apps-spec-page-example render
```

The command emits:

- `dist/index.html`
- runtime assets under `dist/assets/`

No Astro runtime is used.
